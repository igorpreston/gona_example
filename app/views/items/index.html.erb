<% content_for :title, 'Items' %>
<% content_for :heading, 'Items' %>
<% buttons = [
  link_to(new_item_path, class: 'btn btn-primary', data: { turbo_frame: :modal }) do
    icon(Rails.application.config.fa_style, 'plus', class: 'mr-2') + 'Add item'
  end
] %>

<%= render layout: 'layouts/menu' do |f| %>
  <%= render partial: 'menus/header', locals: { buttons: } %>

  <div class="block">
    <div class="content">
      <%= render partial: 'items/items', locals: { items: @items } %>

      <div class="mt-8">
        <%= render partial: 'components/pagination', locals: { pagy: @pagy } %>
      </div>
    </div>
  </div>
<% end %>
