<% content_for :title, 'New category' %>
<% content_for :heading, 'New category' %>

<%= render layout: 'layouts/modal', locals: {
  width_class: 'md:max-w-[540px]',
  position_class: 'items-center',
  height_class: 'h-auto'
} do |f| %>
  <div class="block">
    <%= render 'form', category: %>
  </div>
<% end %>
