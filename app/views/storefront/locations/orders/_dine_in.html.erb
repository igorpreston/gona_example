<%= turbo_frame_tag :order do %>
  <% if order.added_items_count.positive? %>
    <div class="fixed bottom-0 right-0 left-0 md:left-auto pointer-events-none z-50">
      <div class="md:content">
        <%= link_to location_order_path(location, order),
          data: { turbo_frame: :modal },
          class:
          'pointer-events-auto select-none md:rounded-full bg-gray-900 md:mb-4 shadow-lg mx-auto cursor-pointer flex text-center justify-center items-center hover:bg-gray-800 transition w-full md:w-auto' do
        %>
          <div class="py-3 md:py-4 px-6 md:px-8 text-center text-white font-normal text-lg">
            <span class="rounded-full h-[40px] min-w-[40px] inline-flex items-center justify-center bg-white mr-1 text-gray-900 py-2 px-3">
              <%= order.total_quantity %>
            </span>
            • Add to order
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
<% end %>
