<% content_for :title, 'Modifiers' %>
<% content_for :heading, 'Modifiers' %>
<% buttons = [
  link_to(new_modifier_path, class: 'btn btn-primary', data: { turbo_frame: :modal }) do
    icon(Rails.application.config.fa_style, 'plus', class: 'mr-2') + 'Add modifier'
  end
] %>

<%= render layout: 'layouts/menu' do |f| %>
  <%= render partial: 'menus/header', locals: { buttons: } %>

  <div class="block">
    <div class="content">
      <%= render partial: 'modifiers/modifiers', locals: { modifiers: @modifiers } %>

      <div class="mt-8">
        <%= render partial: 'components/pagination', locals: { pagy: @pagy } %>
      </div>
    </div>
  </div>
<% end %>
